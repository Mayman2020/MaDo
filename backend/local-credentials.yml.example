# Copy this file to: backend/local-credentials.yml (gitignored).
# Only needed if your local Postgres password is not the default chain (DB_PASS / KICK_LOCAL_DB_PASSWORD / DB_PASSWORD, default admin).
# Do not commit real secrets.
#
# spring:
#   datasource:
#     password: "your-local-postgres-password"
